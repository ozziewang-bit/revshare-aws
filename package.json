{
  "name": "revshare-aws",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test lambda/revshare-api/tests/*.test.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "ulid": "^2.4.0"
  }
}
